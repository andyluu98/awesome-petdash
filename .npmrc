# Hoist binaries (typescript/tsc, vite, electron-builder, etc.) to the workspace
# root node_modules/.bin so every package's build script can find them.
# Without this, fresh `pnpm install` clones fail with "'tsc' is not recognized"
# when building workspace dependency packages.
shamefully-hoist=true
